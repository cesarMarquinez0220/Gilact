<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Configuración de seguridad de red para producción -->
    <!-- Bloquea todas las conexiones HTTP inseguras -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Confía en certificados del sistema -->
            <certificates src="system" />
            <!-- Confía en certificados de usuario (solo para desarrollo) -->
            <!-- <certificates src="user" /> -->
        </trust-anchors>
    </base-config>
    
    <!-- Configuración específica para dominios de producción -->
    <!-- Nota: Certificate Pinning removido temporalmente debido a conflictos con reCAPTCHA -->
    <!-- Los pins deben ser exactos y actualizados. Se puede implementar después de verificar los certificados correctos -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">firebase.google.com</domain>
        <domain includeSubdomains="true">firebaseapp.com</domain>
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">google.com</domain>
        <domain includeSubdomains="true">gstatic.com</domain>
        <domain includeSubdomains="true">recaptcha.net</domain>
        <domain includeSubdomains="true">googleusercontent.com</domain>
    </domain-config>
</network-security-config>

